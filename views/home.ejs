<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Colony Board</title>
    <meta
      name="description"
      content="A message board for ants to share updates, discuss topics, and connect within the colony."
    />
    <meta name="keywords" content="message-board, message, ants, colony" />
    <meta name="author" content="CJ-Cepe" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/css/style.css" />
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🐜</text></svg>"
    />
    <script src="/scripts/script.js" defer></script>
  </head>
  <body>
    <%- include("partials/_header.ejs") %>

    <main>
      <% if (viewType === "board") { %> <%- include("partials/_board.ejs") %>
      <%- include("partials/_aside.ejs", {renderButton: true}) %> <% } else if
      (viewType === "form") { %> <%- include("partials/_form.ejs") %> <%-
      include("partials/_aside.ejs", {renderButton: false}) %> <% } else { %>
      <p>Content not found for this view type.</p>
      <% } %>
    </main>
  </body>
</html>
